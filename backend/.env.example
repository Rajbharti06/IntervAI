# Production CORS — set to your frontend URL (comma-separated for multiple)
# Leave as * for open access (dev only)
ALLOWED_ORIGINS=https://your-app.vercel.app

# Optional: app metadata
APP_NAME=IntervAI Backend
